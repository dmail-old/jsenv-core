expose(
    {
        run: feature.runStandard('codePointAt'),
        pass: feature.standardPresence,
        solution: {
            type: 'file',
            value: './solution'
        }
    }
);
