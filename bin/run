#!/usr/bin/env node

require('../index.js');

var engine = global.engine;
var fileLocation = process.argv[2];
var fileURL = engine.nodeFile(fileLocation);

process.argv.splice(2, 1);

engine.main(fileURL);
