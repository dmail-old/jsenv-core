#!/usr/bin/env node

var run = require('../run.js');
var path = require('path');
var fileLocation = process.argv[2];
var directoryPath = path.dirname(path.resolve(process.cwd(), fileLocation));

process.chdir(directoryPath);
process.argv.splice(2, 1);

run(fileLocation);